{
  "name": "chrome-extension",
  "version": "1.0.0",
  "description": "A Chrome extension that captures URLs and enriches them with Clay data via Zapier.",
  "main": "background.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0"
  }
}
